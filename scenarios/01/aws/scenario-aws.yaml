scenario: '1.0'

info:
  code: '01'
  title: 'From WEB to the serverless NoSQL database'

test:

  input:
    type: 'http'
    count: '100.000'
    payload: '{ "id" : "uuid" }'

  infrastructure:
    - 'AWS Lambda'
    - 'AWS DynamoDB'

  output:
    metrics:
      - "Average execution time"
      - "Average execution cost"
    deploy:
      - "Script for infrastructure creation"
      - "Script for infrastructure removal"
    test:
      - "Script for scenario execution"